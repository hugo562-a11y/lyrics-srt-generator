# 2026-07-28 開發進度

## 新增功能

### 播放頭操作強化
- **拖曳播放頭**：點擊時間尺可直接拖曳移動；在波形區點擊播放頭線（±6px）也可拖曳
- **方向鍵微調**：← / → 移動 1 幀（1/30 秒）；Shift+← / Shift+→ 移動 5 幀
- **Home / End 鍵**：Home 跳到音檔開頭，End 跳到音檔結尾
- 方向鍵與 Home/End 在 Entry / Text / Spinbox 有焦點時不攔截

### 新增歌詞插入播放頭位置
- 按「＋新增」改為在播放頭當前時間點插入，並依時間排序，不再固定加在末尾

### 歌詞分配場景（全面改版）
- **場景欄位**：歌詞表格「文字」欄右側新增「場景」欄，直接顯示已分配的場景編號，點格即彈出選單
- **互斥分配**：一句歌詞只能屬於一個場景；選新場景自動從舊場景移除（移動語意），點 ✓ 取消分配
- **Index 記錄**：改用 `lyric_seg_ids`（segment index）記錄分配關係，編輯歌詞文字後分配不失效；舊存檔自動以文字比對回退相容

### 分鏡表視圖操作
- **重置視角**：改為自動計算所有場景＋角色卡片的邊界，縮放並平移使全部內容可見（最小 20%、最大 200%）
- **重新整理**（新按鈕）：放在「重置視角」右邊，將所有場景卡片重設回 4 欄格狀預設位置

## 修正項目

### 小螢幕右側被截斷
- `geometry` 改為 `min(1360, screenWidth-60) × min(960, screenHeight-80)` 自適應螢幕尺寸
- `minsize` 降至 900×640，初始 sash 位置透過 `after(150)` 確保右側欄有 300px 寬

### 載入專案時間軸空白
- `load_project` 補呼叫 `waveform.set_audio()` + `_load_waveform_async()`，載入後聲波正常顯示

### 拖曳邊界優先抓字尾
- `_find_handle` 改為同距離時優先選 `end` handle，解決相鄰兩句重疊時誤抓下一句字首

### 刪除按鈕無效
- 原本「刪除」只是軟刪除（標灰），改為真實移除句子（`pop`），場景的 `lyric_seg_ids` 同步往前修正索引；復原可救回

### 雙擊場景欄崩潰
- `_begin_edit` 修正：第 5 欄（場景）雙擊不再觸發 IndexError，改為開啟分配選單邏輯

---

# 2026-07-15 開發進度

## 修正項目

### 預覽窗無限變寬
- **問題**：使用 `tk.Label` + `image=` 後，每次設定圖片會觸發 Configure 事件，導致 widget 不斷自動放大。
- **解法**：將 `tk.Label` 改為 `tk.Canvas` 繪製預覽圖，widget 大小由 grid 佈局決定，不再受圖片影響。
- **相關提交**：`523b458`

### 預覽窗尺寸無法適配面板
- **問題**：預覽窗使用固定 480×270 渲染尺寸，文字底部被裁切。
- **解法**：`_preview_dimensions()` 改為讀取 Canvas 實際 `winfo_width/height`，渲染尺寸自動適配面板大小。
- **相關提交**：`f5cd446`

## 新增功能

### 逐字動畫效果重寫
- **目標**：12 種動畫效果各有獨特的逐字動態表現，不再全部像打字機。
- **做法**：為每種動畫加入 `karaoke_reveal`（依序浮現）搭配各自獨立的視覺效果。
- **各動畫效果**：

| 動畫 | 效果描述 |
|------|---------|
| 逐字點亮 | 字依序亮起金黃光暈，scale 微幅脈衝 |
| 彈跳聚焦 | 字從上方逐字掉落並彈跳 |
| 滑入淡出 | 整行一起滑入（不逐字），純 enter/leave 動畫 |
| 電影柔和 | 整行柔和淡入（不逐字），低光暈 |
| 暴風雨 | 字逐字閃爍震動，搖晃幅度隨 active 增大 |
| 脈衝擴散 | 字逐個產生擴散環 |
| 水波震盪 | 字逐字上下左右波動 |
| 雷射掃過 | 雷射線逐字掃過，命中字發紅光+加粗描邊 |
| 氣泡彈出 | 字逐個從下方彈出，squeeze 變形 |
| 殘影拖曳 | 字逐字帶殘影滑入 |
| 閃爍霓虹 | 字逐個霓虹閃爍，紅/綠/藍隨機色 |
| 粒子爆破 | 字逐個散開再聚攏 |
- **相關提交**：`730d450`、`9199147`

## 技術備註
- `karaoke_reveal = _smooth(max(0, min(1, (local - karaoke * char_progress) / (.15 / speed))))`
  - `karaoke`：字數越多時越長，最短 0.55s、最長為 duration * 70%
  - `char_progress`：0.0（第一字）→ 1.0（最後一字）
  - 搭配 `_smooth()` 三次方緩動，每字依序浮現
- 滑入淡出/電影柔和不使用 `karaoke_reveal`，維持整行 enter/leave 效果
