faster-whisper>=1.1.0
soundfile>=0.12.1
numpy>=1.26.0

# Optional NVIDIA GPU acceleration is downloaded automatically only when needed:
# nvidia-cublas-cu12
# nvidia-cudnn-cu12

# Optional forced alignment (more accurate timing, downloaded on demand):
# whisperx>=3.3.0
